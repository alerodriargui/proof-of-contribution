services:
  - type: web
    name: proof-of-contribution
    runtime: static
    buildCommand: echo "No build step required"
    staticPublishPath: .
    routes:
      - type: redirect
        source: /
        destination: /app/
