services:
  - type: web
    name: proof-of-contribution
    runtime: static
    buildCommand: echo "No build step required"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /contributors/*
        destination: /contributors/index.html
